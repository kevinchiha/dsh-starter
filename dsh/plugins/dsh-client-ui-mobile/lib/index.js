/**
 * Host half. Deliberately empty: this plugin only styles the browser.
 * The row has to exist so the cordis loader can resolve it.
 */
export const name = 'dsh-client-ui-mobile'
export function apply() {}
